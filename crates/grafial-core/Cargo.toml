[package]
name = "grafial-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Core engine for Grafial Bayesian belief graphs"

[dependencies]
grafial-frontend = { path = "../grafial-frontend" }
grafial-ir = { path = "../grafial-ir" }
thiserror.workspace = true
smallvec.workspace = true
rustc-hash.workspace = true
lazy_static = { workspace = true, optional = true }
tracing = { workspace = true, optional = true }
serde = { workspace = true, optional = true }
serde_json = { workspace = true, optional = true }
bincode = { workspace = true, optional = true }
rayon = { workspace = true, optional = true }
crossbeam-channel = { workspace = true, optional = true }
parking_lot = { workspace = true }
num_cpus = { workspace = true, optional = true }
cranelift-codegen = { workspace = true, optional = true }
cranelift-frontend = { workspace = true, optional = true }
cranelift-jit = { workspace = true, optional = true }
cranelift-module = { workspace = true, optional = true }
cranelift-native = { workspace = true, optional = true }
cranelift-object = { workspace = true, optional = true }

[dev-dependencies]
criterion = { workspace = true }

[features]
# Maturity: supported | experimental | scaffold/baseline only (see README)
default = []
tracing = ["dep:tracing"]
serde = ["dep:serde", "dep:serde_json"]
bincode = ["dep:bincode", "serde"]
rayon = ["dep:rayon"]
# experimental
jit = [
    "dep:cranelift-codegen",
    "dep:cranelift-frontend",
    "dep:cranelift-jit",
    "dep:cranelift-module",
    "dep:cranelift-native",
    "dep:lazy_static",
]
# supported
vectorized = []
# supported
parallel = [
    "dep:rayon",
    "dep:crossbeam-channel",
    "dep:num_cpus",
]
# experimental
aot = [
    "dep:cranelift-codegen",
    "dep:cranelift-frontend",
    "dep:cranelift-module",
    "dep:cranelift-native",
    "dep:cranelift-object",
    "dep:lazy_static",
]
# scaffold/baseline only
storage-experimental = []
storage-dense-index = ["storage-experimental"]
storage-soa = ["storage-experimental"]
# experimental
simd-kernels = []
# scaffold/baseline only
gpu-kernels = []

[[bench]]
name = "vectorized_evidence"
harness = false

[[bench]]
name = "parallel_execution"
harness = false

[[bench]]
name = "numeric_kernels"
harness = false
